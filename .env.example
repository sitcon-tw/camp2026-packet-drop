# Drizzle
DATABASE_URL=local.db

# Game server (port for Bun WS)
WS_PORT=8080

# Comma-separated hostnames allowed by Vite preview/dev servers
# Example: ALLOWED_HOSTS=packet-drop-game-3rutb9-7c939f-151-246-244-25.sslip.io
ALLOWED_HOSTS=

# ACK barrier timing
# T_ACK: ms each player stays armed (all N must overlap within window to fire)
T_ACK=600000
# T_AFK: ms before idle player is auto-armed (default 2 × T_ACK if unset)
T_AFK=1200000
# WRONG_PENALTY: ms cooldown after wrong submit
WRONG_PENALTY=3000
# CORRUPTION_CHANCE: per-fragment corruption probability (0.0 - 1.0)
CORRUPTION_CHANCE=0.4
