services:
  packet-drop:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: 3000
      WS_PORT: 8080
    expose:
      - '3000'
      - '8080'
    restart: unless-stopped
